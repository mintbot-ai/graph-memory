{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "graph-memory configuration",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "embedding_model": {
      "type": "string",
      "default": "BAAI/bge-small-en-v1.5",
      "description": "FastEmbed model loaded locally for semantic recall."
    },
    "ingest_turns": {
      "type": "string",
      "enum": ["all", "explicit-only"],
      "default": "all",
      "description": "Whether every primary conversation turn is extracted automatically."
    },
    "recall_limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 8,
      "description": "Maximum graph facts injected into a turn."
    }
  }
}
